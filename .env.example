# 系统服务地址，通常为项目的基础访问地址
SYSTEM_API_URL=http://your-project-url.com

# 商城名称，可用于显示标题等（可在系统中修改）
SHOP_NAME=积分商城

# 商城前台访问地址（可在系统中修改）
SHOP_URL=http://your-shop-url.com

# 项目绑定的主机地址，开发环境通常为 http://127.0.0.1
HOST=http://127.0.0.1

# 项目监听的端口号，例如 8080、9000 等
LISTEN=8080

# 项目重启时用于回调的地址，通常与 HOST 相同
RE_OPEN_HOST=http://127.0.0.1

# 安全校验用的 API 密钥，建议为 32 位随机字符串
SECURE_API_KEY=your-32-char-api-key

# Redis 连接配置（缓存/队列用）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Redis 密码，如无则留空
REDIS_PASSWORD=

# MySQL 数据库连接配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password
